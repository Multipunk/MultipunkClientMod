TempShitSender = {
    id = 0
}

function TempShitSender:new()
    TempShitSender.id = 0
end


function TempShitSender:send(vector)
    print("tdsaf")
    -- local file = io.open("net_test" + tostring(TempShitSender.id) + "zalupa.txt", "w")
    -- file:write(tostring(vector:ToString()))
    -- file:close()
end